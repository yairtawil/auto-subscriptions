{
  "name": "auto-subscriptions",
  "version": "1.0.0",
  "description": "A typescript class and propery decorators to manager all subscriptions",
  "main": "dist/auto-subscriptions.js",
  "typings": "dist/auto-subscriptions.d.ts",
  "scripts": {
    "build": "rimraf dist && tsc",
    "build:dev": "tsc -p tsconfig.dev.json -w -listEmittedFiles",
    "prepublish": "npm run build"
  },
  "keywords": [],
  "author": "Yair Tawil",
  "license": "MIT",
  "files": [
    "dist/*"
  ],
  "devDependencies": {
    "rimraf": "^2.6.2",
    "typescript": "^2.9.2"
  }
}
